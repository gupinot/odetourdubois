---
import Layout from "../layouts/Layout.astro";
const base = import.meta.env.BASE_URL.replace(/\/?$/, '/');
---

<Layout title="Biographie" theme="dark">
  <div class="page-content">
    <div class="image-wrap">
      <img
        src={`${base}images/pages/Biographie.jpg`}
        alt="Biographie Roland Pinot"
        class="page-img"
      />
    </div>
  </div>
</Layout>

<style>
  .page-content {
    flex: 1;
    min-height: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 2.5rem 0;
  }

  .image-wrap {
    flex: 1;
    min-height: 0;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .page-img {
    display: block;
    height: 100%;
    max-height: min(100%, 1050px);
    width: auto;
    max-width: 100%;
    object-fit: contain;
  }
</style>
